package finalProject
import std.console.*

main(): Int64 {
    //var code = Console.stdIn.readToEnd().getOrThrow().toString()
    //Console.stdOut.write(CJCodeTool.refactorVariable(code, "testClass.test1", "test", "suc"))
    //Console.stdOut.write(CJCodeTool.foldConstant(code))
    serverStart()
    return 0
}